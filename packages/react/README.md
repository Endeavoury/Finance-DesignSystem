# @finance-design/react

Thin `@lit/react` adapters for the Web Components in `@finance-design/design-system`. This package adds typed React props and custom events; it contains no separate component implementation.
