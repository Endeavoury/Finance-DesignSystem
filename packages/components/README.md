# @endeavoury/kanosis

Product-neutral Lit Web Components for Kanosis. Import the package root to register all 58 elements, a grouped entry point such as `/button`, `/forms`, `/interaction`, `/overlays`, `/navigation`, or `/tree` for selective registration, and `/styles.css` for the optional global foundation.
